package com.studentfactory.generated.config;

import org.springframework.context.annotation.Configuration;

/**
 * WebConfig - CORS and security config has moved to SecurityConfig.
 * BCryptPasswordEncoder bean is defined in SecurityConfig.
 * This file is kept as a placeholder for additional MVC configuration.
 */
@Configuration
public class WebConfig {
  // CORS is configured in SecurityConfig.corsConfigurationSource()
  // BCryptPasswordEncoder is defined in SecurityConfig.passwordEncoder()
}